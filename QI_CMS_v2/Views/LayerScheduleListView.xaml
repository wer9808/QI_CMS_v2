<UserControl x:Class="QI_CMS_v2.Views.LayerScheduleListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QI_CMS_v2.Views"
             xmlns:properties="clr-namespace:QI_CMS_v2.ControlProperties"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ListViewItemContainer" TargetType="ListViewItem">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            Margin="{TemplateBinding Margin}"
                            CornerRadius="3">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="True" />
                        <Condition Property="IsMouseOver" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="Orange" />
                    <Setter Property="BitmapEffect">
                        <Setter.Value>
                            <DropShadowBitmapEffect ShadowDepth="2" />
                        </Setter.Value>
                    </Setter>
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="False" />
                        <Condition Property="IsMouseOver" Value="True" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="Orange" />
                    <Setter Property="BitmapEffect">
                        <Setter.Value>
                            <DropShadowBitmapEffect ShadowDepth="0" />
                        </Setter.Value>
                    </Setter>
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="True" />
                        <Condition Property="IsMouseOver" Value="True" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="Orange" />
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected" Value="False" />
                        <Condition Property="IsMouseOver" Value="False" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="#333333" />
                    <Setter Property="BitmapEffect">
                        <Setter.Value>
                            <DropShadowBitmapEffect ShadowDepth="2" />
                        </Setter.Value>
                    </Setter>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Name}"
                       Foreground="White"
                       Margin="5"/>
            <Button Grid.Column="1" Content="추가" FontSize="10"
                    Command="{Binding AddScheduleCommand}"
                    Style="{StaticResource BorderButtonStyle}"
                    BorderBrush="Transparent"
                    Background="Transparent" Foreground="White"
                    properties:CustomButtonProperties.CornerRadius="0"
                    properties:CustomButtonProperties.MouseOverBackground="{StaticResource ColorSecondary}"
                    Effect="{x:Null}"
                    Margin="0" Padding="5">
            </Button>
        </Grid>
        <DockPanel Grid.Row="1">
            <TextBlock DockPanel.Dock="Top" Text="연속 재생" Foreground="White" Margin="5"/>
            <ListView ItemsSource="{Binding DefaultItems}" SelectedItem="{Binding SelectedSchedule}" Background="{StaticResource ColorPrimary}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Duration, StringFormat='재생시간: {0:F3}'}" Foreground="White" Margin="30,5"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat='생성일: {0}'}" Foreground="White" Margin="30,5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemContainer}">
                        <Setter Property="MaxWidth" Value="400"/>
                        <Setter Property="MaxHeight" Value="200"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Margin" Value="10"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </DockPanel>
        <DockPanel Grid.Row="2">
            <TextBlock DockPanel.Dock="Top" Text="주기적 재생" Foreground="White" Margin="5"/>
            <ListView ItemsSource="{Binding RepeatItems}" SelectedItem="{Binding SelectedSchedule}" Background="{StaticResource ColorPrimary}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{Binding StartTime, StringFormat='주기: {0}'}" Foreground="White" Margin="30,5"/>
                            <TextBlock Text="{Binding Duration, StringFormat='재생시간: {0:F3}'}" Foreground="White" Margin="30,5"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat='생성일: {0}'}" Foreground="White" Margin="30,5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemContainer}">
                        <Setter Property="MaxWidth" Value="400"/>
                        <Setter Property="MaxHeight" Value="200"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Margin" Value="10"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </DockPanel>
        <DockPanel Grid.Row="3">
            <TextBlock DockPanel.Dock="Top" Text="일회성 재생" Foreground="White" Margin="5"/>
            <ListView ItemsSource="{Binding DisposableItems}" SelectedItem="{Binding SelectedSchedule}" Background="{StaticResource ColorPrimary}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{Binding StartTime, StringFormat='시작시간: {0}'}" Foreground="White" Margin="30,5"/>
                            <TextBlock Text="{Binding Duration, StringFormat='재생시간: {0:F3}'}" Foreground="White" Margin="30,5"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat='생성일: {0}'}" Foreground="White" Margin="30,5"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemContainer}">
                        <Setter Property="MaxWidth" Value="400"/>
                        <Setter Property="MaxHeight" Value="200"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Margin" Value="10"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </DockPanel>
    </Grid>
</UserControl>
